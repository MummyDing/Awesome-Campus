<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_search"
        android:icon="@mipmap/ic_search_white"
        android:title="@string/search"
        android:visible="false"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/menu_notify"
        android:icon="@mipmap/ic_notify_none"
        android:title="@string/menu_notify"
        android:visible="false"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/menu_new_goods"
        android:icon="@mipmap/ic_add_white"
        android:title=""
        android:visible="false"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/menu_user_info"
        android:icon="@mipmap/ic_perm_identity_white"
        android:visible="false"
        app:showAsAction="ifRoom"></item>

    <item
        android:id="@+id/menu_search_goods"
        android:icon="@mipmap/ic_search_white"
        android:title="@string/search"
        android:visible="false"
        app:showAsAction="ifRoom" />
</menu>