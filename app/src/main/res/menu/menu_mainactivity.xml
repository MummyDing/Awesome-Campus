<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_search" android:icon="@mipmap/ic_search_white"
        app:showAsAction="ifRoom"
        android:visible="false"
        app:actionViewClass="android.support.v7.widget.SearchView" android:title="@string/search" />
    <item android:id="@+id/menu_notify" android:icon="@mipmap/ic_notify_none"
        android:visible="false"
        app:showAsAction="ifRoom" android:title="@string/menu_notify" />
    <item
        android:id="@+id/menu_new_goods" android:icon="@mipmap/ic_add_white"
        android:visible="false"
        app:showAsAction="ifRoom" android:title="@string/menu_notify"
        />
</menu>